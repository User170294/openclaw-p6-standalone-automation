## Sesión 2026-02-25 11:21 UTC-3
- Proyecto(s): OT-1844 / PO 4519143302
- Decisiones:
  - Se adopta revisión de comunicaciones por ventana semanal (últimos 7 días) + consolidación con histórico local.
  - OWA se usa para revisión incremental; la carpeta local de .msg queda como base histórica más confiable.
  - Se estableció formato de “resumen” como informe detallado (no solo ejecutivo) para estado del servicio según comunicaciones.
- Próximos pasos:
  - Ejecutar corte semanal OWA + índice local y actualizar `projects/OT-1844/MAIL_LOG.md`.
  - Consolidar enviados/recibidos sin duplicados y mantener reporte en `projects/OT-1844/reports/`.
  - Construir matriz semaforizada de riesgos/responsables para seguimiento operativo.
- Bloqueos/Riesgos:
  - Automatización Power Automate por categoría no quedó estable en este entorno (timing/categorías trigger).
  - Riesgo de desfase entre OWA y archivo local si no se sincronizan nuevos correos del día.
## Sesi�n [2026-02-25 16:01 UTC-3]
- Proyecto(s): OT-1844 (RAG)
- Decisiones:
  - Se aplic� Sprint A en RAG: retrieval h�brido (vector+BM25) con fusi�n RRF.
  - Se corrigi� normalizaci�n de proyecto para aceptar 1844 y OT-1844.
  - Benchmark ajustado para medir vg_rerank + 
el@3 por query.
- Pr�ximos pasos:
  - Robustecer benchmark con nDCG@3/MRR@3 y gold set de consultas.
  - Afinar query de "hitos entrega marzo" con filtros por documentos de cronograma.
- Bloqueos/Riesgos:
  - Ruido de codificaci�n CP1252 en consola Windows (Unicode) puede falsear salidas de scripts.

## Sesión 2026-02-25 17:21 UTC-3
- Proyecto(s): OT-1844 / Pipeline RAG
- Decisiones:
  - Se implementó logging JSONL de métricas RAG con conteo de tokens (cl100k_base) en logs/rag_metrics/.
  - Se implementó caché BM25 por colección con validación doble (doc_count + md5 primeros 50 docs).
  - Se creó worker persistente TCP loopback (project_query_worker.py) y cliente (worker_client.py) para evitar recarga de modelos por query.
  - Se agregó hardening: limpieza automática de worker.pid.json stale y endpoint de shutdown limpio ({"shutdown":true} → {"ok":true}).
  - Se registró preferencia operativa: “trae la carpeta al frente” = abrir carpeta en Explorador local del equipo.
- Próximos pasos:
  - Integrar delegación al worker en search_project.py (si worker activo, delegar; si no, fallback local).
  - Integrar mismo patrón en oco_proyecto.py.
  - Mantener script 
eingest_collection.py para reingesta limpia por colección cuando se actualicen chunks.
- Bloqueos/Riesgos:
  - En Windows, señales del SO pueden no limpiar PID en todos los escenarios; mitigado con cleanup desde cliente y shutdown por endpoint.
  - Persisten tiempos de arranque altos si se usa modo one-shot sin delegar al worker.

## Sesión 2026-02-25 21:10 UTC-3
- Proyecto(s): Finanzas / Informe M.Munizaga
- Decisiones:
  - Se creó el proyecto `projects/finanzas` con estructura base (README, MEMORY, LOG, INDEX).
  - Se corrigieron fórmulas en `Indices Financieros` del Excel mensual para manejar `#N/D` y `#¡DIV/0!` con `SI.ERROR` a nivel de término y de división.
  - Se definió enfoque inicial: primero cargar/inventariar datos antes de modelar automatización.
- Próximos pasos:
  - Revisar consistencia de nombres de cuentas (alias contables) entre `Indices Financieros` y `CW20251114C887`.
  - Extraer modelo y métricas clave desde PBIX más reciente para diseño del pipeline automático.
  - Consolidar vista mensual homologada (Sep–Ene) para base de reporte automatizado.
- Bloqueos/Riesgos:
  - Variantes de nombres de cuentas generan búsquedas incompletas en BUSCARX.
  - Riesgo de resultados en 0 por claves faltantes si no se normaliza diccionario contable.
