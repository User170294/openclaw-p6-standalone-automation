## Sesión 2026-02-24 (12:01 – 12:15 GMT-3)

- **Proyecto(s):** OT-1844

---

### Decisiones
- **RAG verificada:** El `code 1` reportado por PowerShell es un falso positivo (tqdm escribe en stderr). El script `search_project.py` retorna `EXIT: 0` cuando se suprime stderr con `2>$null`. La RAG está operativa con 2334 chunks.
- **Método correcto de ejecución:** Siempre usar `2>$null` al correr scripts RAG en PowerShell para evitar falsos errores.
- **Esquema A-3 documentado:** Detalle completo extraído de ChromaDB directamente (no via script search) por chunks truncados en RAG. Método: `col.get(where={"doc_id": ...})` con ordenamiento por página.

### Cambios de estado
- Ninguno nuevo en el proyecto — sesión de consulta/verificación.

### Pendientes para próxima sesión
- RFIs 011841, 011842, 011844, 011845, 011846 — sin respuesta del cliente
- Verificar avance S9 (cierre 01-mar-2026) y si se cerró brecha de −0.82%
- Confirmar inicio actividad A2670 (Consolas Centrales ZM-1)
- Monitorear A2980 y A3280 (float 0h)
- Generar informe S9 con pipeline habitual

### Lecciones aprendidas
- Para consultas con detalle completo de documentos: usar `query_chroma_pintura.py` (o método `col.get()` directo) en lugar de `search_project.py` cuando se necesite el texto íntegro de un documento específico.

## Sesión 2026-02-24 (15:29 – 17:28 GMT-3)
- Proyecto(s): OT-1844
- Decisiones:
  - Se reinició la base OT-1844 y se reconstruyó en modo acumulativo (ingesta por carpetas BBTT, BBCC, Calidad y Planificación).
  - Se incorporaron archivos XER de Planificación y se confirmó baseline: `SPC-2220-PS-PMS-011840_..._Rev.B.xer`.
  - Convención oficial de semanas fijada para reportes: `W##` (W08 = 16–22 feb 2026, luego consecutivo).
- Próximos pasos:
  - Evaluar si se ingieren carpetas faltantes: `General`, `Ingeniería Fabricación`, `001_OT 1844`.
  - Si se aprueba, correr ingesta acumulativa + reindexación Chroma con `--reset`.
- Bloqueos/Riesgos:
  - Cobertura documental parcial hasta decidir incorporación de carpetas faltantes (62 PDFs detectados).
